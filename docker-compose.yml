
# services:
#   checklist:
#     build:
#       context: ./app
#       dockerfile: Dockerfile
#     ports:
#       - "3010:3000"
#     volumes:
#       - ./data:/app/data
#     environment:
#       - DATABASE_URL=file:/app/data/local.db
#     restart: unless-stopped



services:
  checklist:
    build: ./app
    ports:
      - "3010:3000" 
    volumes:
      - ./data:/app/data   
    environment:
      - DATABASE_URL=file:/app/data/local.db  
      - ORIGIN=http://100.114.69.27:3010      # <-- Add this line
    restart: unless-stopped